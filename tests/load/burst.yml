config:
  target: "http://127.0.0.1:3000"
  phases:
    - duration: 15
      arrivalRate: 35
      name: "Burst Test"

scenarios:
  - name: "Health Check Burst"
    flow:
      - get:
          url: "/api/health"
